# exit: 0
conman VERSION  model 0.4  tokenizer claude-local
entry: .   mode: stack
config: conman.json

LOAD ORDER
  #    kind         source                              lines         tokens
  b1   memory       CLAUDE.md                           1-5               59
  b2   rule-always  .claude/rules/legacy-globs.md       1-7               44
  b3   rule-always  .claude/rules/no-close-fence.md     1-6               34
  b4   rule-always  .claude/rules/paths-number.md       1-6               26
  b5   rule-always  .claude/rules/unparseable-scope.md  1-6               33
  b6   skill-index  .claude/skills                      1-3               31
  --------------------------------------------------------------------------
  total                                                                  227

BUDGET
  budget          2000
  safety margin   10%   ->  effective 1800
  stack total     227
  delta           -1573  (under budget)

SAVINGS
  redundant tokens: 0 (0% of stack)

FINDINGS  (3 error, 4 warn)
  error  frontmatter
         .claude/rules/no-close-fence.md:1-1
         frontmatter opens with `---` but has no closing `---`; the block is read as prose and any `paths` scope is ignored, so the rule loads always-on
  error  frontmatter
         .claude/rules/paths-number.md:1-4
         `paths` is a number, not a string or a list of strings; the scope yields no globs, so this rule meant to be path-scoped loads always-on instead
  error  frontmatter
         .claude/rules/unparseable-scope.md:1-4
         frontmatter YAML does not parse (Flow sequence in block collection must be sufficiently indented and end with a ]); the `paths` scope cannot be read, so this path-scoped rule silently loads always-on
  warn   frontmatter
         .claude/rules/legacy-globs.md:1-5
         frontmatter sets `globs` but not `paths`; Claude Code path-scopes rules only on `paths`, so this rule loads always-on
  warn   frontmatter
         .claude/rules/paths-string.md:1-4
         `paths` is a bare string, not a YAML list; conman reads it as a single glob, but the documented form is a list and some parsers drop a scalar
  warn   frontmatter
         .claude/skills/broken-yaml/SKILL.md:1-5
         frontmatter YAML does not parse (Tabs are not allowed as indentation); `name` and `description` cannot be read
  warn   frontmatter
         .claude/skills/no-desc/SKILL.md:1-3
         skill frontmatter has no usable `description`; the startup listing shows this skill with an empty description

NOTES
  - rule .claude/rules/legacy-globs.md sets `globs` but not `paths`; Claude Code path-scopes rules only on `paths`, so this rule loads always-on
  - rule .claude/rules/paths-string.md is path-scoped (src/**); did not match entry .
  - rule .claude/rules/valid.md is path-scoped (src/**); did not match entry .

RESULT  fail
  - 3 frontmatter findings at error severity
