# Skill-index budget fixture

Four skills with long descriptions. The startup skill index is over the tiny
`budget.skillIndex` cap set in conman.json, so the skill-index-budget finding
fires at warn.
