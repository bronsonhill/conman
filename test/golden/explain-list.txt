# exit: 0
conman VERSION - finding types

  dead-reference      Reference that does not resolve
  duplication         Duplicated block
  frontmatter         Malformed rule / skill frontmatter
  lint-duplication    Lint rule the tooling already enforces
  max-skills          Too many skills in the startup index
  per-file-budget     Single file over the per-file budget
  skill-index-budget  Skill index over the token budget
  stale-boilerplate   Unmodified /init boilerplate
  unlinked-copy       Unlinked CLAUDE.md / AGENTS.md copy
  value-conflict      Direct value conflict
  vehicle-fit         Oversized always-loaded block

Run `conman explain <id>` for the full entry.
