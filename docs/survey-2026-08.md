# conman "in the wild" survey — 2026-08

Generated by `scripts/survey.mjs` (standalone tooling, not CI, not the
pinned fixture corpus). Every `conman map` run here is offline; only the
script clones.

## Method

- **conman**: `0.1.0`, resolution model `0.3`, built from this branch's HEAD.
- **Budget**: built-in default, `--budget 12000` with the 0.10 safety margin (10,800-token gate line).
- **Candidates**: 294 public repos, from candidate list `docs/survey-candidates-2026-08.txt`.
  Forks and archived repos dropped. Discovery queries: `path:.claude/settings.json`, `path:.claude/settings.local.json`, `path:.claude/commands`, `filename:CLAUDE.md`, `filename:AGENTS.md`.
- **Sample**: 10 repos, seed `conman-survey-2026-08`, picked by stable `sha256(seed + "\0" + repo)` ordering — a rerun over the same candidate list and seed picks the same repos.
- **Per repo**: `git clone --depth 1`, `conman map --json`, capture, then delete the clone before the next.
- **Mapped**: 9. **Skipped**: 1.

## Aggregate

| measure | result |
|---------|--------|
| repos mapped | **9** (of 10 sampled; 1 skipped) |
| entry points resolved | **88** |
| redundant tokens, % of resolved stack (per repo) | **median 0%, mean 0.01%** |
| repos with ≥1 error-severity value conflict | **1 of 9 — 11.1%** |
| median resolved stack (across all entry points) | **17,803 tokens** |
| repos with ≥1 over-budget entry point | **5 of 9 — 55.6%** |
| entry points over budget | **82 of 88 — 93.2%** |

## Per-repo rows

| repo | entry points | resolved tokens | redundant tokens | redundant % | over-budget EPs | value-conflict EPs |
|------|-------------:|----------------:|-----------------:|------------:|----------------:|-------------------:|
| `Fatih0234/mattpocock-skills-pi` | 1 | 1,011 | 0 | 0% | 0 | 0 |
| `SuperGokou/hyclaw` | 26 | 363,806 | 0 | 0% | 26 | 0 |
| `alirezarezvani/claude-skills` | 22 | 686,930 | 0 | 0% | 22 | 12 |
| `amirjundi/hsma` | 28 | 504,067 | 0 | 0% | 28 | 0 |
| `gohugoio/hugo` | 2 | 1,556 | 0 | 0% | 0 | 0 |
| `netdata/netdata` | 3 | 62,457 | 71 | 0.11% | 3 | 0 |
| `rust-lang/rust` | 2 | 6,943 | 0 | 0% | 0 | 0 |
| `sanjaykims/remarkabler` | 3 | 80,370 | 0 | 0% | 3 | 0 |
| `thedotmack/claude-mem` | 1 | 303 | 0 | 0% | 0 | 0 |

## Skipped

| repo | reason |
|------|--------|
| `unclecode/crawl4ai` | empty-stack (no entry points / zero tokens) |

## Reproducing / the full run

This sample was produced with:

```
node scripts/survey.mjs --candidates <candidate-list> --limit 10 --seed conman-survey-2026-08
```

The captain's full run (durable checkout, clones persist there):

```
cd ~/Documents/Repositories/conman
git pull
npm run build
node scripts/survey.mjs --discover-only --pages 10
node scripts/survey.mjs --candidates docs/survey-candidates-2026-08.txt --limit 100 --seed conman-survey-2026-08
```

Expect ~45-90 min wall time (network-bound) and up to ~25 GB of transient
clone traffic; peak disk stays near a single repo because each clone is
deleted before the next. Review this file and the README diff, then commit.
