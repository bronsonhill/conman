# Cloned fixture repos — fetched on demand at pinned SHAs, never committed.
fixtures/repos/
node_modules/
dist/
*.log
.claude/settings.local.json
.DS_Store

# Budget-calibration sweep outputs — regenerated by the manual harness.
eval/budget-calibration/results/
