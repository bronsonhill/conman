# exit: 0
conman VERSION map  model 0.6
config: conman.json
entry points discovered: 2

  entry         mode           tokens    delta   findings              result
  .             stack             227    -1573   3 error, 4 warn       FAIL
  src           stack             227    -1573   3 error, 4 warn       FAIL

discovered via a path-scoped rule (no CLAUDE.md / AGENTS.md of their own):
  src

path-scoped rules that did not match every entry point:
  rule .claude/rules/paths-string.md is path-scoped (src/**); did not match 2 entry points
  rule .claude/rules/valid.md is path-scoped (src/**); did not match 2 entry points

path-scoped rules that matched no entry point (dead scope):
  rule .claude/rules/paths-string.md is path-scoped (src/**); matched no discovered entry point
  rule .claude/rules/valid.md is path-scoped (src/**); matched no discovered entry point

repo rollup: 454 tokens across 2 entry points
redundant tokens: 0 (0% of stack)

RESULT  fail
  .
    - 3 frontmatter findings at error severity
  src
    - 3 frontmatter findings at error severity
