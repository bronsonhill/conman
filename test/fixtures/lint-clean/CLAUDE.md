# Lint-clean fixture

The formatter config carries the mechanical rules; this file does not restate them.

- Explain non-obvious control flow in a comment.
- Keep public functions small and named for what they return.
- Run `make check` before pushing.
