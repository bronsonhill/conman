# Dead-ref-clean fixture

- See @setup.md for environment setup.
- The entry point is `src/index.ts`.
- State lives in @tanstack/react-query caches; charts use @visx/scale.
- **Components come from @mui/material**
- Run `npm run build` before every commit.
