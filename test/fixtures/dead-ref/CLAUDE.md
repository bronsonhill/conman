# Dead-ref fixture

Setup and conventions:

- See @./missing-setup.md for environment setup.
- The frontend is built on @superset-ui/core and @xyflow/react.
- Import conventions live at @docs/style-notes.
- Architecture notes live in `docs/architecture.md`.
- Architecture: read the [notes](docs/architecture.md) for the layout.
- The [module readme](docs/README.md) covers the src tree.
- Upstream reference: [the spec](https://example.com/spec.md).
- Run `npm run lint` before every commit.
- Then run npm run typecheck, written in bare prose without backticks.
- The entry point is `src/index.ts`.
