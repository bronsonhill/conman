# Rule-entry discovery fixture

The only memory file in this repo is at the root. `src/main`, `src/renderer`,
`src` and `docs` are entry points only because `.claude/rules/` files
path-scope to them. See `src/map.test.ts`.
