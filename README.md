# conman

Context manager: a tool to help developers manage the AI-agent context embedded
in a repository — memory files (`CLAUDE.md` / `AGENTS.md`), skills, rules, and
settings — keeping it reviewed, proportionate, and cost-aware rather than letting
it grow additively and unsupervised.

Early exploration. Vision and scope are still being worked out.
