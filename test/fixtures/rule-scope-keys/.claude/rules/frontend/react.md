---
description: Nested rule file; Claude Code discovers `.claude/rules/` recursively, so this loads and path-scopes just like a top-level rule
paths:
  - src/renderer
---

React components keep hooks above early returns.
