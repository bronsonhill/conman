---
applyTo: "src/backend/**"
---

# Backend API instructions

GitHub's docs scope instruction files under nested
`.github/instructions/**/*.instructions.md`. This one sits a directory deep so
the fixture exercises recursive discovery in both the resolver and `conman map`.
